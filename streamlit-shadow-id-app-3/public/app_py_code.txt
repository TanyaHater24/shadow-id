import streamlit as st
from openai import OpenAI
from datetime import datetime, date
import random

# ═══════════════════════════════════════════════════════════════
# 🔮 SHADOW ID — Теневой Паспорт Личности
# ═══════════════════════════════════════════════════════════════

st.set_page_config(
    page_title="SHADOW ID • Теневой Паспорт",
    page_icon="🔮",
    layout="centered",
    initial_sidebar_state="collapsed"
)

# ═══════════════════════════════════════════════════════════════
# ⭐ ГЕНЕРАЦИЯ ЗВЁЗД — 120 штук с разными размерами и скоростями
# ═══════════════════════════════════════════════════════════════

stars_html = ""
for i in range(120):
    x = random.uniform(0, 100)
    y = random.uniform(0, 100)
    size = random.uniform(1, 3.5)
    duration = random.uniform(2, 7)
    delay = random.uniform(0, 5)
    opacity = random.uniform(0.3, 1)
    colors = ["#ffffff", "#e0e0ff", "#ffe0f0", "#e0f0ff", "#f0e0ff", "#00ffff", "#ff00ff"]
    color = random.choice(colors)
    stars_html += f'<div class="star" style="left:{x}%;top:{y}%;width:{size}px;height:{size}px;background:{color};animation-duration:{duration}s;animation-delay:{delay}s;opacity:{opacity};box-shadow:0 0 {size*3}px {color};"></div>\n'

shooting_stars = ""
for i in range(5):
    top = random.uniform(5, 40)
    delay = random.uniform(0, 12)
    duration = random.uniform(1.5, 3)
    shooting_stars += f'<div class="shooting-star" style="top:{top}%;animation-delay:{delay}s;animation-duration:{duration}s;"></div>\n'

# ═══════════════════════════════════════════════════════════════
# 💎 CSS — MOBILE-FIRST + ЗВЁЗДНОЕ НЕБО + НЕОН
# ═══════════════════════════════════════════════════════════════

st.markdown(f"""
<style>
@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Inter:wght@300;400;500;600&family=JetBrains+Mono:wght@400;700&display=swap');

*, *::before, *::after {{box-sizing: border-box;}}

.stApp {{
    background: radial-gradient(ellipse at 50% 0%, #0d0520 0%, #07010f 40%, #020008 100%) !important;
    min-height: 100vh;
    overflow-x: hidden;
}}

#MainMenu, footer, header, .stDeployButton,
.st-emotion-cache-1avcm0n, .st-emotion-cache-18ni7ap,
[data-testid="stToolbar"], [data-testid="stDecoration"],
[data-testid="stStatusWidget"] {{display: none !important;}}

.block-container {{
    padding: 1rem 1rem 3rem !important;
    max-width: 700px !important;
}}

.starfield {{
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    pointer-events: none;
    z-index: 0;
    overflow: hidden;
}}

.star {{
    position: absolute;
    border-radius: 50%;
    animation: twinkle ease-in-out infinite alternate;
}}

@keyframes twinkle {{
    0% {{opacity: 0.1; transform: scale(0.5);}}
    50% {{opacity: 1; transform: scale(1.2);}}
    100% {{opacity: 0.2; transform: scale(0.8);}}
}}

.shooting-star {{
    position: absolute;
    left: 100%;
    width: 120px;
    height: 2px;
    background: linear-gradient(90deg, rgba(255,255,255,0.9), transparent);
    border-radius: 50%;
    animation: shoot linear infinite;
    opacity: 0;
}}

@keyframes shoot {{
    0% {{transform: translateX(0) translateY(0) rotate(-35deg); opacity: 0;}}
    5% {{opacity: 1;}}
    15% {{opacity: 1;}}
    20% {{transform: translateX(-120vw) translateY(60vh) rotate(-35deg); opacity: 0;}}
    100% {{opacity: 0;}}
}}

.nebula {{
    position: fixed;
    border-radius: 50%;
    filter: blur(80px);
    pointer-events: none;
    z-index: 0;
    animation: nebula-drift 20s ease-in-out infinite alternate;
}}

.nebula-1 {{
    top: -10%; right: -10%;
    width: 50vw; height: 50vw;
    max-width: 400px; max-height: 400px;
    background: radial-gradient(circle, rgba(139,0,255,0.15) 0%, transparent 70%);
}}

.nebula-2 {{
    bottom: -10%; left: -10%;
    width: 40vw; height: 40vw;
    max-width: 350px; max-height: 350px;
    background: radial-gradient(circle, rgba(255,0,128,0.1) 0%, transparent 70%);
    animation-delay: -10s;
}}

.nebula-3 {{
    top: 40%; left: 50%;
    width: 30vw; height: 30vw;
    max-width: 300px; max-height: 300px;
    background: radial-gradient(circle, rgba(0,255,255,0.08) 0%, transparent 70%);
    animation-delay: -5s;
}}

@keyframes nebula-drift {{
    0% {{transform: translate(0, 0) scale(1);}}
    50% {{transform: translate(20px, -20px) scale(1.1);}}
    100% {{transform: translate(-10px, 10px) scale(0.95);}}
}}

.content-wrapper {{
    position: relative;
    z-index: 10;
}}

.logo-section {{
    text-align: center;
    padding: 1.5rem 0 1rem;
}}

.crystal-ball {{
    font-size: 3.5rem;
    display: inline-block;
    animation: float 4s ease-in-out infinite, glow-pulse 3s ease-in-out infinite;
}}

@keyframes float {{
    0%, 100% {{transform: translateY(0);}}
    50% {{transform: translateY(-12px);}}
}}

@keyframes glow-pulse {{
    0%, 100% {{filter: drop-shadow(0 0 15px rgba(139,0,255,0.6));}}
    50% {{filter: drop-shadow(0 0 35px rgba(255,0,255,0.9)) drop-shadow(0 0 60px rgba(0,255,255,0.4));}}
}}

.logo-title {{
    font-family: 'Orbitron', sans-serif;
    font-size: 2.2rem;
    font-weight: 900;
    letter-spacing: 0.25em;
    margin: 0.8rem 0 0.3rem;
    background: linear-gradient(135deg, #ff00ff, #00ffff, #ff88ff, #88ffff);
    background-size: 300% 300%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradient-shift 4s ease infinite;
}}

@keyframes gradient-shift {{
    0% {{background-position: 0% 50%;}}
    50% {{background-position: 100% 50%;}}
    100% {{background-position: 0% 50%;}}
}}

.logo-sub {{
    font-family: 'Inter', sans-serif;
    font-weight: 300;
    font-size: 0.85rem;
    color: rgba(255,255,255,0.45);
    letter-spacing: 0.4em;
    text-transform: uppercase;
    margin: 0;
}}

.divider {{
    width: 60%;
    max-width: 300px;
    height: 1px;
    margin: 1.5rem auto;
    background: linear-gradient(90deg, transparent, rgba(139,0,255,0.6), rgba(0,255,255,0.6), transparent);
    position: relative;
}}

.divider::after {{
    content: '✦';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: rgba(139,0,255,0.8);
    font-size: 0.7rem;
    background: #07010f;
    padding: 0 0.8rem;
}}

.input-card {{
    background: rgba(15, 5, 30, 0.7);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(139, 0, 255, 0.2);
    border-radius: 16px;
    padding: 1.5rem;
    margin: 1rem 0;
    position: relative;
    overflow: hidden;
}}

.input-card::before {{
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, #ff00ff, #00ffff, transparent);
    opacity: 0.6;
}}

.input-label {{
    font-family: 'Orbitron', sans-serif;
    font-size: 0.65rem;
    color: rgba(0, 255, 255, 0.7);
    letter-spacing: 0.3em;
    text-transform: uppercase;
    text-align: center;
    margin-bottom: 1rem;
}}

.stDateInput > div > div {{
    background: rgba(0,0,0,0.4) !important;
    border: 1px solid rgba(139,0,255,0.3) !important;
    border-radius: 12px !important;
    color: #fff !important;
    font-family: 'JetBrains Mono', monospace !important;
    transition: all 0.3s ease !important;
}}

.stDateInput > div > div:focus-within {{
    border-color: rgba(255,0,255,0.6) !important;
    box-shadow: 0 0 20px rgba(139,0,255,0.2) !important;
}}

.stDateInput label {{
    color: rgba(255,255,255,0.7) !important;
    font-family: 'Inter', sans-serif !important;
    font-weight: 400 !important;
    font-size: 0.9rem !important;
}}

.stButton > button {{
    width: 100% !important;
    padding: 1rem 2rem !important;
    font-family: 'Orbitron', sans-serif !important;
    font-size: 0.85rem !important;
    font-weight: 700 !important;
    letter-spacing: 0.2em !important;
    color: #fff !important;
    background: linear-gradient(135deg, #7b00e0 0%, #c800ff 50%, #00c8ff 100%) !important;
    background-size: 200% 200% !important;
    border: none !important;
    border-radius: 14px !important;
    cursor: pointer !important;
    position: relative !important;
    overflow: hidden !important;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1) !important;
    animation: btn-bg 4s ease infinite !important;
    box-shadow: 0 4px 25px rgba(139,0,255,0.4) !important;
    text-transform: uppercase !important;
    margin-top: 0.5rem !important;
}}

@keyframes btn-bg {{
    0%, 100% {{background-position: 0% 50%;}}
    50% {{background-position: 100% 50%;}}
}}

.stButton > button:hover {{
    transform: translateY(-2px) !important;
    box-shadow: 0 8px 35px rgba(139,0,255,0.5), 0 0 60px rgba(255,0,255,0.2) !important;
}}

.stButton > button:active {{
    transform: translateY(1px) scale(0.98) !important;
}}

.stButton > button::after {{
    content: '';
    position: absolute;
    top: -50%; left: -60%;
    width: 40%;
    height: 200%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.15), transparent);
    transform: skewX(-20deg);
    animation: button-shine 3s ease-in-out infinite;
}}

@keyframes button-shine {{
    0% {{left: -60%;}}
    40%, 100% {{left: 120%;}}
}}

.result-card {{
    background: rgba(10, 2, 20, 0.85);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(139,0,255,0.15);
    border-radius: 16px;
    padding: 1.5rem;
    margin-top: 1.5rem;
    animation: result-in 0.6s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    line-height: 1.8;
}}

.result-card::before {{
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, #ff00ff, #00ffff, transparent);
}}

@keyframes result-in {{
    0% {{opacity: 0; transform: translateY(20px);}}
    100% {{opacity: 1; transform: translateY(0);}}
}}

.result-card p,
.result-card li {{
    font-family: 'Inter', sans-serif;
    font-size: 0.95rem;
    line-height: 1.85;
    color: rgba(255,255,255,0.88);
}}

.result-card strong {{
    color: #e580ff;
}}

.result-card h1, .result-card h2, .result-card h3 {{
    font-family: 'Orbitron', sans-serif;
    color: #00e5ff;
    font-size: 0.9rem;
    letter-spacing: 0.1em;
    margin-top: 1.2rem;
}}

.result-block {{
    border-left: 2px solid rgba(139,0,255,0.3);
    padding-left: 1rem;
    margin: 1.2rem 0;
}}

.text-reveal {{
    animation: text-appear 1s ease-out;
}}

@keyframes text-appear {{
    0% {{opacity: 0; filter: blur(4px);}}
    100% {{opacity: 1; filter: blur(0);}}
}}

.loader-wrap {{
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 2.5rem 0;
    gap: 1.2rem;
}}

.loader-eye {{
    font-size: 2.5rem;
    animation: eye-pulse 1.2s ease-in-out infinite;
}}

@keyframes eye-pulse {{
    0%, 100% {{transform: scale(1); filter: drop-shadow(0 0 10px rgba(139,0,255,0.5));}}
    50% {{transform: scale(1.15); filter: drop-shadow(0 0 25px rgba(255,0,255,0.8));}}
}}

.loader-bar {{
    width: 160px;
    height: 3px;
    background: rgba(255,255,255,0.08);
    border-radius: 3px;
    overflow: hidden;
    position: relative;
}}

.loader-bar::after {{
    content: '';
    position: absolute;
    top: 0; left: -40%;
    width: 40%;
    height: 100%;
    background: linear-gradient(90deg, transparent, #ff00ff, #00ffff, transparent);
    animation: bar-slide 1.2s ease-in-out infinite;
}}

@keyframes bar-slide {{
    0% {{left: -40%;}}
    100% {{left: 100%;}}
}}

.loader-msg {{
    font-family: 'Inter', sans-serif;
    font-size: 0.8rem;
    color: rgba(255,255,255,0.4);
    letter-spacing: 0.15em;
    animation: fade-pulse 2s ease-in-out infinite;
}}

@keyframes fade-pulse {{
    0%, 100% {{opacity: 0.4;}}
    50% {{opacity: 0.8;}}
}}

.footer-text {{
    text-align: center;
    padding: 2.5rem 0 1rem;
    font-family: 'Inter', sans-serif;
    font-size: 0.7rem;
    color: rgba(255,255,255,0.2);
    letter-spacing: 0.15em;
}}

@media (max-width: 430px) {{
    .block-container {{
        padding: 0.5rem 0.8rem 2rem !important;
    }}
    .logo-title {{
        font-size: 1.6rem !important;
        letter-spacing: 0.15em !important;
    }}
    .logo-sub {{
        font-size: 0.7rem !important;
        letter-spacing: 0.25em !important;
    }}
    .crystal-ball {{
        font-size: 2.8rem !important;
    }}
    .input-card {{
        padding: 1.2rem 1rem !important;
        margin: 0.8rem 0 !important;
    }}
    .input-label {{
        font-size: 0.6rem !important;
    }}
    .stButton > button {{
        padding: 0.9rem 1rem !important;
        font-size: 0.75rem !important;
        letter-spacing: 0.12em !important;
        border-radius: 12px !important;
    }}
    .result-card {{
        padding: 1.2rem 1rem !important;
        margin-top: 1rem !important;
    }}
    .result-card p, .result-card li {{
        font-size: 0.88rem !important;
        line-height: 1.7 !important;
    }}
    .result-card h1, .result-card h2, .result-card h3 {{
        font-size: 0.8rem !important;
    }}
    .divider {{
        margin: 1rem auto !important;
    }}
    .nebula {{
        filter: blur(50px) !important;
    }}
}}

@media (min-width: 431px) and (max-width: 768px) {{
    .block-container {{
        padding: 1rem 1.2rem 2rem !important;
    }}
    .logo-title {{
        font-size: 1.9rem !important;
        letter-spacing: 0.18em !important;
    }}
    .crystal-ball {{
        font-size: 3rem !important;
    }}
    .result-card {{
        padding: 1.4rem !important;
    }}
}}

@supports (-webkit-touch-callout: none) {{
    .stApp {{
        min-height: -webkit-fill-available;
    }}
    .starfield {{
        position: fixed;
        height: -webkit-fill-available;
    }}
}}

@media (hover: none) {{
    .stButton > button:hover {{
        transform: none !important;
        box-shadow: 0 4px 25px rgba(139,0,255,0.4) !important;
    }}
}}
</style>

<!-- STARS -->
<div class="starfield">
    {stars_html}
    {shooting_stars}
</div>

<div class="nebula nebula-1"></div>
<div class="nebula nebula-2"></div>
<div class="nebula nebula-3"></div>

<div class="content-wrapper">
""", unsafe_allow_html=True)

# ═══════════════════════════════════════════════════════════════
# 🔮 СИСТЕМНЫЙ ПРОМТ
# ═══════════════════════════════════════════════════════════════

SYSTEM_PROMPT = """
Ты — Мастер Матрицы Судьбы и Нумеролог-Психолог "Shadow ID". Ты видишь кармические задачи людей. Твой тон — жесткий, но справедливый (как "Зеркало Правды"). Без сюсюканья.

ТВОЯ ЗАДАЧА:
На основе полученной даты рождения составить Теневой Портрет личности.

СТРУКТУРА ОТВЕТА (Используй Markdown):

👤 **АРХЕТИП ЛИЧНОСТИ:**
(Определи Аркан по дню рождения. Опиши суперсилу и харизму человека).

🌑 **ТВОЯ ТЕНЬ (Что блокирует деньги и любовь):**
(Включи "злого психолога". Расскажи, как человек саботирует свой успех. Пиши хлестко, попадай в боль).

🐍 **КАРМИЧЕСКИЙ ХВОСТ:**
(Метафорически опиши ошибку прошлого, которую он повторяет сейчас).

💡 **СОВЕТ-КЛЮЧ:**
(Одно конкретное действие, чтобы выйти из "минуса" в "плюс").

ФИНАЛ:
Заканчивай ответ фразой: "Тень увидена. Теперь выбор за тобой."
"""

# ═══════════════════════════════════════════════════════════════
# 🎰 ИНТЕРФЕЙС
# ═══════════════════════════════════════════════════════════════

st.markdown("""
<div class="logo-section">
    <div class="crystal-ball">🔮</div>
    <div class="logo-title">SHADOW ID</div>
    <p class="logo-sub">теневой паспорт личности</p>
</div>
<div class="divider"></div>
""", unsafe_allow_html=True)

API_KEY = "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"

st.markdown('<div class="input-card"><div class="input-label">✦ введи дату рождения ✦</div>', unsafe_allow_html=True)

birth_date = st.date_input(
    "📅 Дата рождения",
    value=date(1990, 1, 1),
    min_value=date(1920, 1, 1),
    max_value=date.today(),
    format="DD.MM.YYYY"
)

reveal_button = st.button("🔮 РАСКРЫТЬ ТЕНЬ", use_container_width=True)

st.markdown('</div>', unsafe_allow_html=True)

# ═══════════════════════════════════════════════════════════════
# ⚡ ГЕНЕРАЦИЯ
# ═══════════════════════════════════════════════════════════════

if reveal_button:
    formatted_date = birth_date.strftime("%d.%m.%Y")

    loader = st.empty()
    loader.markdown("""
    <div class="loader-wrap">
        <div class="loader-eye">👁️</div>
        <div class="loader-bar"></div>
        <div class="loader-msg">считываю матрицу судьбы...</div>
    </div>
    """, unsafe_allow_html=True)

    try:
        client = OpenAI(
            api_key=API_KEY,
            base_url="https://api.groq.com/openai/v1"
        )

        response = client.chat.completions.create(
            model="llama-3.3-70b-versatile",
            messages=[
                {"role": "system", "content": SYSTEM_PROMPT},
                {"role": "user", "content": f"Дата рождения: {formatted_date}"}
            ],
            temperature=0.9,
            max_tokens=2000
        )

        result = response.choices[0].message.content

        loader.empty()

        sections = result.split("\n\n")
        formatted = ""
        for section in sections:
            section_html = section.replace("\n", "<br>")
            formatted += f'<div class="result-block text-reveal">{section_html}</div>'

        st.markdown(f"""
        <div class="result-card">
            {formatted}
        </div>
        """, unsafe_allow_html=True)

        with st.expander("📜 Текстовая версия (для копирования)", expanded=False):
            st.markdown(result)

    except Exception as e:
        loader.empty()
        error_msg = str(e)
        st.markdown(f"""
        <div class="result-card" style="border-color: rgba(255,50,50,0.3);">
            <p style="color: #ff6b6b; font-family: 'Inter', sans-serif;">⚠️ Ошибка: {error_msg}</p>
            <p style="color: rgba(255,255,255,0.5); font-family: 'Inter', sans-serif; font-size: 0.85rem;">
                Попробуй ещё раз через несколько секунд.
            </p>
        </div>
        """, unsafe_allow_html=True)

st.markdown('</div>', unsafe_allow_html=True)

st.markdown("""
<div class="footer-text">
    ✧ SHADOW ID • 2025 ✧<br>
    матрица судьбы • нумерология • теневой портрет
</div>
""", unsafe_allow_html=True)
